{
  "name": "switch-mcp-server",
  "version": "0.9.0",
  "type": "module",
  "license": "UNLICENSED",
  "main": "dist/stdio.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/stdio.js",
    "test": "npm run build && npx tsx test/test-chunking.ts && npx tsx test/test-mcp.ts && npx tsx test/test-scenarios.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
